import React, { Fragment } from 'react'
import { Popover, Transition } from '@headlessui/react'
import { MenuIcon, XIcon } from '@heroicons/react/outline'

const navigation = [
  { name: 'Projects', href: '/archive/' },
  { name: 'Education', href: '/' },
]

export default function Hero () {
  return (
      <div></div>
  )
}